<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC 
			"-//mybatis.org//DTD Mapper 3.0//EN" 
			"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
			
<mapper namespace="com.itbank.model.Member2DAO">

	<select id="login" parameterType="member" resultType="member">
		select * from member2
			where
				userId = #{userId} and
				userPw = #{userPw}
		
	</select>

	<insert id="insert" parameterType="member">
		insert into member2
			(userId, userPw, userName, birth, email, gender, address)
			values
			(
				#{userId},
				#{userPw},
				#{userName},
				#{birth},
				#{email},
				#{gender},
				#{address}
			)
	</insert>
	
	<select id="selectOne" parameterType="string" resultType="member">
		select * from member2
			where userId = #{userId}
	
	</select>
	
	<update id="updatePassword" parameterType="hashmap">
		update member2 set
			userPw = #{userPw}
		where
			userId = #{userId}
	</update>
	
	<update id="updateMember" parameterType="member">
		update member2 set 
			userName= #{userName}, 
			birth= #{birth}, 
			email= #{email}, 
			gender= #{gender}, 
			address= #{address} 
		where 
			userId= #{userId}
	
	</update>
	
	<delete id="delete" parameterType="string">
		delete member2 where userId = #{userId}
	
	</delete>
	
<!-- 	<select id="selectBoardCount" resultType="int" parameterType="hashmap"> -->
<!-- 		select count(*) from board  -->
<!-- 			where deleted='n' -->
<!-- 			<if test="search.equals('') == false"> -->
<!-- 				and ${type} like '%${search}%'			 -->
<!-- 			</if> -->
<!-- 	</select> -->
	
	
<!-- 	<select id="selectAll" parameterType="hashmap" resultType="board"> -->
<!-- 		select * from board -->
<!-- 			where  -->
<!-- 				deleted = 'n'  -->
<!-- 				<if test="search.equals('') == false"> -->
<!-- 					and ${type} like '%${search}%'			 -->
<!-- 				</if> -->
<!-- 			order by  -->
<!-- 				idx desc -->
<!-- 			offset #{offset} rows -->
<!-- 			fetch first #{perPage} rows only -->
<!-- 	</select> -->
	
</mapper>



